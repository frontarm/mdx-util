const mdxTableOfContents = require('./index')

test("adds tableOfContents when not defined in MDX file", async () => {
  
})

test("does not add tableOfContents when already defined in MDX file", async () => {
  
})